% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tangent_plane3d.R
\name{tangent_plane3d}
\alias{tangent_plane3d}
\title{Tangent plane and normal of a surface z = f(x,y) (numeric)}
\usage{
tangent_plane3d(
  F,
  point,
  h = 1e-04,
  plot = FALSE,
  x_window = 4,
  y_window = 4,
  z_window = 4,
  grid = 50,
  plane_window = 1,
  vec_N_factor = 1,
  surface_opacity = 0.85,
  plane_opacity = 0.7,
  colors = list(surface = "Viridis", plane = "Reds", xcut = "black", ycut = "black",
    point = "blue", normal = "red"),
  show_surface_grid = FALSE,
  surface_grid_color = "rgba(0,0,0,0.35)",
  surface_grid_width = 1,
  show_axis_grid = TRUE,
  axis_grid_color = "#e5e5e5",
  axis_grid_width = 1,
  scene = list(aspectmode = "data", xaxis = list(title = "x"), yaxis = list(title = "y"),
    zaxis = list(title = "z")),
  bg = list(paper = "white", plot = "white")
)
}
\arguments{
\item{F}{Function \code{F(x,y)} returning \code{z}.}

\item{point}{Vector \code{c(a,b)} where the tangent plane is computed.}

\item{h}{Step size for centered finite differences.}

\item{plot}{Logical; if \code{TRUE}, produces a 3D figure.}

\item{x_window, y_window}{Half-widths in \code{x} and \code{y} for the surface.}

\item{z_window}{Half-height for fixing the \code{z}-range around \code{f(a,b)}.}

\item{grid}{Number of divisions per axis for the surface mesh.}

\item{plane_window}{Half-width of the rectangle where the tangent plane \code{g} is drawn.}

\item{vec_N_factor}{Length of the normal vector segment (scale).}

\item{surface_opacity, plane_opacity}{Opacities of surface and plane (0–1).}

\item{colors}{List of colors/scales: \code{list(surface = "Viridis", plane = "Reds",
  xcut = "black", ycut = "black", point = "blue", normal = "red")}.
\itemize{
\item \code{surface}, \code{plane}: plotly colorscales (e.g. "Viridis", "Cividis", "Blues").
\item \code{xcut}, \code{ycut}, \code{point}, \code{normal}: simple colors.
}}

\item{show_surface_grid}{Logical; show mesh grid over the surface.}

\item{surface_grid_color}{Color of the surface mesh grid (e.g. \code{"rgba(0,0,0,0.35)"}).}

\item{surface_grid_width}{Width of the surface mesh grid.}

\item{show_axis_grid}{Logical; show grid on x,y,z axes.}

\item{axis_grid_color}{Color of the axis grid.}

\item{axis_grid_width}{Width of the axis grid.}

\item{scene}{Base 3D scene settings (axis/grid options are added).}

\item{bg}{Background colors: \code{list(paper="white", plot="white")}.}
}
\value{
A list with \code{fx}, \code{fy}, \code{f0}, \code{normal_unit},
\code{normal_raw}, \code{plane_fun}, \code{plane_coeff}.
}
\description{
Tangent plane and normal of a surface z = f(x,y) (numeric)
}
