% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/line_integral_vector2d.R
\name{line_integral_vector2d}
\alias{line_integral_vector2d}
\title{Vector line integral in R^2 with field + trajectory visualization}
\usage{
line_integral_vector2d(
  F,
  r,
  a,
  b,
  plot = TRUE,
  n_curve = 600,
  grid_n = 15,
  padding = 0.15,
  h = NULL,
  method = c("adaptive", "simpson"),
  n_simpson = 1000,
  arrow_scale = 0.08,
  normalize_bias = 1,
  field_color = "rgba(0,0,0,0.55)",
  field_width = 1.8,
  traj_palette = "RdBu",
  traj_width = 5,
  show_markers = FALSE,
  scene = list(aspectmode = "data", xaxis = list(title = "x"), yaxis = list(title = "y"),
    zaxis = list(title = "z")),
  bg = list(paper = "white", plot = "white")
)
}
\arguments{
\item{F}{function(x,y) -> numeric(2) returning c(Fx,Fy).}

\item{r}{function(t)   -> numeric(2) returning c(x,y).}

\item{a, b}{scalars with b > a (parameter limits).}

\item{plot}{logical; if TRUE, draws an interactive plotly figure.}

\item{n_curve}{integer; samples of the path for plotting.}

\item{grid_n}{integer; grid resolution per axis for the field.}

\item{padding}{numeric; margin added around path bounding box for field grid.}

\item{h}{numeric or NULL; step for finite differences of r'(t). If NULL, chosen automatically.}

\item{method}{"adaptive" (stats::integrate) or "simpson".}

\item{n_simpson}{integer (even); subintervals if method="simpson".}

\item{arrow_scale}{overall arrow length as fraction of plot span (e.g., 0.08).}

\item{normalize_bias}{saturation bias in arrow scaling: F / sqrt(|F|^2 + bias). Default 1.}

\item{field_color}{color (or rgba) for arrow bodies.}

\item{field_width}{numeric line width for arrows.}

\item{traj_palette}{color scale for power along the trajectory (Plotly name or vector of colors).}

\item{traj_width}{line width for trajectory.}

\item{show_markers}{logical; if TRUE, shows markers along trajectory.}

\item{scene, bg}{plotly layout helpers.}
}
\value{
list(value = numeric, samples = data.frame(t,x,y,px,py,speed,power), fig = plotly or NULL)
}
\description{
Computes \eqn{W=\int_a^b F(r(t)) \cdot r'(t)\,dt} and draws:
\itemize{
\item A quiver (arrows) of the vector field \eqn{F(x,y)} on a grid around the path.
\item The trajectory \eqn{r(t)} colored by the power density \eqn{p(t)=F(r(t))\cdot r'(t)}.
}
}
