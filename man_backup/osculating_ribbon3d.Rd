% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/osculating_ribbon.R
\name{osculating_ribbon3d}
\alias{osculating_ribbon3d}
\title{Osculating ribbon of a space curve r(t)}
\usage{
osculating_ribbon3d(
  X,
  Y,
  Z,
  a,
  b,
  h = 1e-04,
  plot = FALSE,
  n_t = 400,
  n_u = 25,
  u_max = 1,
  colorscale = "Blues",
  opacity = 0.35,
  show_curve = TRUE,
  show_centers = TRUE,
  curve_line = list(color = "black", width = 2),
  centers_line = list(color = "red", width = 2),
  show_surface_grid = TRUE,
  surface_grid_color = "rgba(60,80,200,0.25)",
  surface_grid_width = 1,
  show_axis_grid = FALSE,
  scene = list(aspectmode = "data", xaxis = list(title = "x(t)"), yaxis = list(title =
    "y(t)"), zaxis = list(title = "z(t)")),
  bg = list(paper = "white", plot = "white"),
  lighting = list(ambient = 1, diffuse = 0.15, specular = 0, roughness = 1, fresnel = 0),
  tol = 1e-10
)
}
\arguments{
\item{X, Y, Z}{Functions \code{x(t)}, \code{y(t)}, \code{z(t)}.}

\item{a, b}{Endpoints of the interval \code{[a,b]}.}

\item{h}{Step size for centered finite differences.}

\item{plot}{Logical; if \code{TRUE}, draws with \pkg{plotly}.}

\item{n_t, n_u}{Resolution in \code{t} and \code{u}.}

\item{u_max}{Fraction of the radius \eqn{R(t)} to draw (\code{0 < u_max <= 1}). Default \code{1}.}

\item{colorscale}{Plotly colorscale for the ribbon.}

\item{opacity}{Opacity of the ribbon (0–1).}

\item{show_curve}{Logical; draw the curve \eqn{r(t)}.}

\item{show_centers}{Logical; draw the centers \eqn{C(t)}.}

\item{curve_line, centers_line}{Line styles.}

\item{show_surface_grid}{Logical; mesh grid over the surface.}

\item{surface_grid_color, surface_grid_width}{Aesthetics of the surface grid.}

\item{show_axis_grid}{Logical; axis grids.}

\item{scene, bg, lighting}{Scene, background, and lighting settings.}

\item{tol}{Numerical tolerance (curvature ~ 0).}
}
\value{
\code{list} with \code{t_seq, u_seq, Xmat, Ymat, Zmat, curve, centers}.
}
\description{
For each \eqn{t \in [a,b]}, constructs the segment from \eqn{r(t)}
to the center of the osculating circle
\eqn{C(t) = r(t) + N(t)/\kappa(t)} and generates the
\strong{ruled surface}:
\deqn{S(t,u) = r(t) + u\,\frac{N(t)}{\kappa(t)},\quad u\in[0,u_{\max}].}
}
