% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/arc_length3d.R
\name{arc_length3d}
\alias{arc_length3d}
\title{Arc length of a 3D parametric curve (numeric)}
\usage{
arc_length3d(
  X,
  Y,
  Z,
  a,
  b,
  h = 1e-06,
  method_int = c("romberg", "integrate"),
  n_samples = 400,
  plot = FALSE,
  plot_mode = "lines",
  plot_line = list(color = "blue", width = 3, dash = "solid"),
  plot_marker = NULL,
  plot_title = NULL,
  plot_scene = list(xaxis = list(title = "x(t)"), yaxis = list(title = "y(t)"), zaxis =
    list(title = "z(t)")),
  plot_bg = list(paper = "white", plot = "white")
)
}
\arguments{
\item{X, Y, Z}{Functions of \code{t}.}

\item{a, b}{Parameter limits for \code{t}.}

\item{h}{Step size for centered finite differences (numeric derivatives).}

\item{method_int}{\code{"romberg"} (requires \pkg{pracma}) or \code{"integrate"} (base R).}

\item{n_samples}{Number of sample points for plotting (if \code{plot = TRUE}).}

\item{plot}{\code{TRUE}/\code{FALSE}. If \code{TRUE}, plots with \code{plotly}.}

\item{plot_mode, plot_line, plot_marker, plot_title, plot_scene, plot_bg}{Styling parameters passed to \code{plot_curve3d()}.}
}
\value{
Numeric value: arc length.
}
\description{
Computes \eqn{\int_a^b \|\mathbf{r}'(t)\|\,dt} using centered finite
differences for derivatives and numerical integration (Romberg or \code{integrate}).
Optionally, it can also plot the curve with \pkg{plotly}.
}
\examples{
X <- function(t) t^2*cos(t)
Y <- function(t) t^3*sin(3*t)
Z <- function(t) t
arc_length3d(X, Y, Z, 0, 2*pi)
# \donttest{ if (requireNamespace("plotly", quietly = TRUE)) {
#   arc_length3d(X, Y, Z, 0, 2*pi, plot = TRUE,
#              plot_line = list(color="red", width=3),
#              n_samples = 300)
# } }
}
