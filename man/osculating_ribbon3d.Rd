% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/osculating_ribbon3d.R
\name{osculating_ribbon3d}
\alias{osculating_ribbon3d}
\title{Osculating ribbon along a 3D curve}
\usage{
osculating_ribbon3d(
  X,
  Y,
  Z,
  a,
  b,
  h = 1e-04,
  plot = FALSE,
  n_t = 400,
  n_u = 25,
  u_max = 1,
  colorscale = "Blues",
  opacity = 0.35,
  show_curve = TRUE,
  show_centers = TRUE,
  curve_line = list(color = "black", width = 2),
  centers_line = list(color = "red", width = 2),
  show_surface_grid = TRUE,
  surface_grid_color = "rgba(60,80,200,0.25)",
  surface_grid_width = 1,
  show_axis_grid = FALSE,
  scene = list(aspectmode = "data", xaxis = list(title = "x(t)"), yaxis = list(title =
    "y(t)"), zaxis = list(title = "z(t)")),
  bg = list(paper = "white", plot = "white"),
  lighting = list(ambient = 1, diffuse = 0.15, specular = 0, roughness = 1, fresnel = 0),
  tol = 1e-10
)
}
\arguments{
\item{X, Y, Z}{Functions of \code{t} returning \eqn{x(t)}, \eqn{y(t)}, \eqn{z(t)}.}

\item{a, b}{Numeric scalars; interval endpoints for \eqn{t \in [a,b]}.}

\item{h}{Numeric step for finite differences (default \code{1e-4}).}

\item{plot}{Logical; if \code{TRUE}, draws the ribbon using \pkg{plotly}.}

\item{n_t}{Integer; number of samples along the base curve.}

\item{n_u}{Integer; subdivisions across the ribbon width.}

\item{u_max}{Numeric; half-width of the ribbon (in normal direction units).}

\item{colorscale}{Character; Plotly colorscale for the ribbon (default \code{"Blues"}).}

\item{opacity}{Numeric in \eqn{[0,1]}; ribbon opacity.}

\item{show_curve}{Logical; draw the base curve \eqn{\mathbf{r}(t)}.}

\item{show_centers}{Logical; draw the centerline joining ribbon midpoints.}

\item{curve_line}{List; style for the base curve (e.g., \code{list(color="black", width=2)}).}

\item{centers_line}{List; style for the centerline (e.g., \code{list(color="red", width=2)}).}

\item{show_surface_grid}{Logical; show contour grid on the ribbon surface.}

\item{surface_grid_color}{Character; color of the grid lines.}

\item{surface_grid_width}{Numeric; width of the grid lines.}

\item{show_axis_grid}{Logical; show axis background gridlines.}

\item{scene}{List; Plotly 3D scene configuration.}

\item{bg}{List; background colors (\code{paper}, \code{plot}).}

\item{lighting}{List; Plotly lighting options for the surface.}

\item{tol}{Numeric tolerance used for normalizations and zero checks.}
}
\value{
\describe{
\item{\code{data}}{List or tibble with \code{t}, base curve coordinates,
and frame vectors \code{T, N, B}.}
\item{\code{plot}}{Plotly object if \code{plot = TRUE}, else \code{NULL}.}
}
}
\description{
Constructs and (optionally) plots a narrow ribbon that follows a spatial
parametric curve \eqn{\mathbf{r}(t) = (x(t), y(t), z(t))}, based on its Frenet
frame \eqn{(\mathbf{T}, \mathbf{N}, \mathbf{B})}. The ribbon extends a small
distance along the normal direction, forming a “band” that visualizes
curvature and torsion.
}
\examples{
X <- function(t) cos(t)
Y <- function(t) sin(t)
Z <- function(t) 0.2 * t
# osculating_ribbon3d(X, Y, Z, a = 0, b = 4*pi, plot = TRUE)

}
