% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/arc_length3d.R
\name{plot_curve3d}
\alias{plot_curve3d}
\title{Plot a 3D curve with plotly}
\usage{
plot_curve3d(
  data,
  mode = "lines",
  line = list(color = "blue", width = 3, dash = "solid"),
  marker = NULL,
  title = NULL,
  scene = list(xaxis = list(title = "x(t)"), yaxis = list(title = "y(t)"), zaxis =
    list(title = "z(t)")),
  bg = list(paper = "white", plot = "white")
)
}
\arguments{
\item{data}{Tibble with columns \code{t, x, y, z}.}

\item{mode}{Plotly trace mode: \code{"lines"}, \code{"lines+markers"}, etc.}

\item{line}{Line style list (e.g. \code{list(color="blue", width=3, dash="solid")}).}

\item{marker}{Marker style list (or \code{NULL} to omit).}

\item{title}{Plot title.}

\item{scene}{List of 3D axes, e.g. \code{list(xaxis=list(title="x(t)"), ...)}.}

\item{bg}{Background colors: \code{list(paper="white", plot="white")}.}
}
\value{
A plotly object (printed).
}
\description{
Accepts a tibble with columns \code{t, x, y, z} (as produced by \code{curve_sample3d()}).
Requires \pkg{plotly} to be installed.
}
\examples{
data <- curve_sample3d(function(t) 2*cos(t),
                       function(t) 3*sin(t),
                       function(t) t/5, 0, 2*pi, 100)
# \donttest{ if (requireNamespace("plotly", quietly = TRUE)) {
#   plot_curve3d(data, line = list(color="red", width=4))
# } }
}
